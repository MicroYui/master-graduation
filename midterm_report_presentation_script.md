# 硕士学位中期报告 PPT 讲稿

题目：面向动态资源受限多智能体网络的服务质量感知协同通信优化方法研究  
建议时长：约 10 分钟  
对应文件：`template.pptx`

## 第 1 页：封面

各位老师好，我是李一凡。我的中期报告题目是“面向动态资源受限多智能体网络的服务质量感知协同通信优化方法研究”。

本课题关注的是动态边缘服务场景中的协同通信问题。多智能体网络需要同时完成现场数据回传和协同任务推理，但节点移动、链路波动、带宽受限和能量受限会使服务质量难以稳定保障。因此，我的研究主要围绕三个层次展开：首先解决底层服务数据如何可靠传输，其次研究上层协同推理中哪些语义证据值得通信，最后进一步分析真实资源约束下语义通信策略的稳定性和鲁棒性。

## 第 2 页：研究对象与问题

这一页展示的是本课题面向的动态边缘服务通信场景。左侧是多智能体服务网络，包括无人机、移动机器人、边缘摄像头以及承担中继和决策功能的节点；右侧是数据中心和服务质量约束。现场智能体需要把局部感知数据持续回传，同时在协同推理任务中交换语义证据，最终支撑服务质量。

这里的关键困难在于，网络不是稳定的固定基础设施。节点移动会改变拓扑和链路质量，带宽和时延窗口随时间变化，节点能量也会逐渐消耗。如果只追求短期传输收益，容易反复使用少数中继节点，造成能量热点和服务中断；如果在推理任务中无差别发送全部消息，又会带来通信冗余、时延增加和丢包风险。

因此，本课题要解决的问题可以概括为：在链路、带宽和能量持续变化的条件下，如何设计通信决策，使数据能够持续送达，语义证据能够被有效利用，并最终提升多智能体服务质量。

## 第 3 页：研究内容关系

围绕刚才的问题，课题分为三个相互衔接的研究内容。

第一个是服务数据回传，主要研究动态网络中的分布式路由与功率控制。它解决的是“数据能不能持续送达”的问题，为后续语义通信提供底层传输支撑。

第二个是语义证据选择，主要研究在有限通信预算和不可靠链路下，智能体应当选择哪些语义消息进行传输。这里的重点不是传得越多越好，而是在语义价值、通信成本和送达可靠性之间进行选择。

第三个是稳定性分析，也就是把第一个研究内容中的链路质量、功率控制和节点能量变化引入第二个研究内容的语义通信环境中，进一步分析语义消息选择策略在动态资源约束下的表现。这样三个部分形成了从底层传输、上层语义选择到服务质量分析的递进关系。

## 第 4 页：当前研究进展

目前，前两个研究问题已经完成了主要工作，第三个研究问题处于后续推进阶段。

RQ1 服务数据传输已经完成问题建模、算法设计、理论推导、仿真实现和对比实验。相关成果发表于服务计算领域 CCF-B 类国际会议 ICSOC 2025。

RQ2 协同推理服务已经完成离线语义轨迹构造、链路可靠性建模、强化学习策略实现和完整实验。目前实验基于约 3000 条 trace，覆盖主设置、智能体数量、通信预算和关键模块消融。

RQ3 将在此基础上继续推进。后续会把 LEADR 输出的链路质量、功率控制结果、节点能量和存活状态映射到 EviComRL 的语义消息选择环境中，分析动态资源约束下语义通信策略的服务质量表现。

## 第 5 页：RQ1 服务数据传输

第一个研究问题面向高动态 FANET 应急通信场景。当地面通信基础设施受损时，无人机可以作为临时空中中继，将灾区现场数据通过多跳方式回传至数据中心。

这个场景中的建模对象包括无人机移动、无线链路、数据队列和节点能量消耗。优化目标是在保障吞吐、降低丢包的同时，尽可能均衡全网能耗，避免部分节点过早失效。

这里的创新点主要有两点。第一，采用李雅普诺夫漂移加惩罚方法，将长期随机优化问题转化为单时隙在线优化问题，使节点能够根据当前状态实时决策。第二，综合考虑链路质量、队列状态、节点能量和地理进度，以较低复杂度实现路由与功率的联合决策，从而适应高动态、资源受限的服务数据传输环境。

## 第 6 页：LEADR 方法设计

针对 RQ1，本文提出 LEADR 方法。它的核心思想是，把长期能量约束下的全网吞吐优化问题，转化为每个节点在每个时隙内可以独立执行的局部决策。

在执行过程中，节点首先通过状态广播获得邻居的位置、速度、剩余能量和数据队列等信息；然后根据链路收益和通信能耗进行功率控制；接着在链路评估阶段综合速率、能量状态和地理进度；最后选择权重最大的合法邻居作为下一跳。

这个方法不依赖全局控制器，也不需要未来拓扑信息。单节点单时隙复杂度为 O(N_i)，适合机载设备实时运行。同时，通过虚拟能量队列刻画长期能量约束，LEADR 能够减少局部能量热点，在吞吐收益、链路代价和节点剩余能量之间做联合权衡。

## 第 7 页：RQ1 实验结果

RQ1 的实验采用离散事件仿真，任务周期为 3600 秒，对比方法包括 GPSR、LB-GPSR、ACO 和 RLFR。实验从无人机数量、通信范围和飞行速度等方面考察方法表现。

从结果趋势可以看出，LEADR 在不同动态条件下能够维持较高吞吐和较低丢包率，同时改善节点之间的能量均衡。生命周期结果也说明，LEADR 能够减少关键节点过早失效，延长网络持续提供通信服务的时间。

这部分实验说明，在底层链路和能量约束下，通过能量感知的分布式路由与功率控制，可以提升服务数据传输的稳定性和连续性。

## 第 8 页：RQ2 选择性语义通信

第二个研究问题从服务数据传输进一步转向协同推理服务中的语义通信。在多智能体视觉推理任务中，单个智能体通常只能获得局部观测。为了形成更完整的判断，不同智能体需要交换语义证据。

但是，在动态边缘网络中，通信预算、时延和链路可靠性都受到限制，不能简单地发送全部原始观测或全部语义消息。因此，RQ2 关注的问题是：应该选择哪条语义消息、发送给谁，以及采用何种语义粒度。

为此，EviComRL 先使用冻结的 VLM/LLM 离线构造语义轨迹，包括局部证据、多粒度候选消息和接收方信念更新；然后用链路感知强化学习策略进行选择性通信。这样可以把高成本的语义推理和后续策略学习解耦，在有限通信资源下同时考虑推理质量、消息成本、通信时延和送达可靠性。

## 第 9 页：RQ2 实验结果

RQ2 目前已经完成完整实验。实验基于约 3000 条语义轨迹，覆盖主设置、智能体数量、通信预算和关键模块消融。

从主设置结果看，EviComRL 在 Accuracy、Balanced Accuracy、Macro-F1 和 Effective Quality 上取得当前可部署方法中的最好表现。质量--成本折中结果也显示，EviComRL 能够在较高有效质量下控制平均通信开销，优于多数启发式和单步决策基线。

不过，这部分实验也暴露出后续需要继续处理的问题。当前方法仍需要进一步校准通信成本约束、停止策略和链路风险建模。也就是说，语义消息选择本身已经有效，但它还需要放到更贴近动态资源约束的传输环境中重新评估，这正是第三个研究问题要继续解决的内容。

## 第 10 页：存在问题与后续研究

这一页总结当前不足和后续研究方向。前两项工作已经分别解决了底层链路与能量约束、上层语义消息选择两个问题，但两者之间还需要进一步连接。

具体来说，LEADR 中的链路质量、节点能量和传输代价来自物理链路和能量队列动态；而 EviComRL 目前主要在简化链路模型下评估语义消息选择策略。这样虽然可以验证消息选择本身是否有效，但还不能充分回答动态资源受限网络中，语义通信策略在实际传输约束下是否稳定。

后续研究将分三步推进。第一步是约束映射，将 LEADR 输出的链路质量、节点能量和传输代价引入语义通信环境。第二步是策略评估，在动态通信条件下重新评估 EviComRL，观察语义消息选择策略的稳定性。第三步是质量分析，围绕数据持续送达、语义证据利用和最终服务质量，分析不同资源约束下的性能变化。

## 第 11 页：后期计划与预期成果

后期工作主要围绕统一评估框架、鲁棒性实验和论文撰写展开。

2026 年 7 月到 8 月，完成统一评估框架与链路、能量状态映射设计。2026 年 9 月到 10 月，完成动态链路质量与能量约束下的主实验。2026 年 10 月到 11 月，完成结果分析、指标整理和章节撰写。2026 年 11 月到 12 月，完成学位论文初稿和相关成果整理。2026 年 12 月到 2027 年 1 月，完成论文修改、查重与送审准备。

预期成果包括三点：第一，完成链路质量、能量状态和语义消息选择之间的统一建模；第二，得到 EviComRL 在实际传输约束下的鲁棒性分析结论；第三，完成学位论文整体撰写，并形成具备投稿条件的后续研究工作。

## 第 12 页：结束页

以上就是我的中期报告内容。请各位老师批评指正。
